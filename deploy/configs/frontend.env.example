# =====================================================================
# NXT8 frontend/.env — production template
# Copy to /app/frontend/.env on the VPS.
# =====================================================================

# Публичный URL, через который браузер обращается к /api.
# В одно-домейн setup (nginx проксирует /api → :8001) — это сам домен.
REACT_APP_BACKEND_URL=https://nxt8.pro
